Generate Python function code for this tool specification.

TOOL NAME: {tool_name}
DESCRIPTION: {description}
PARAMETERS: {parameters}
REQUIRED COLUMNS: {required_columns}
IMPLEMENTATION PLAN: {implementation_plan}
WHAT IT DOES: {what_it_does}

FUNCTION SIGNATURE (exact -- do not rename):
  def {tool_name}(file_path: str):

TASK RULES:

1. FOLLOW IMPLEMENTATION PLAN EXACTLY -- implement the stated method, never a substitute.
   - ANOVA              -> scipy.stats.f_oneway
   - Correlation        -> scipy.stats.pearsonr or spearmanr
   - Tukey HSD          -> statsmodels.stats.multicomp.pairwise_tukeyhsd
   - t-test (2-group)   -> ttest_ind / mannwhitneyu  (NEVER f_oneway for 2-group)
   - Causal discovery   -> see Algorithm Fidelity rules in system prompt

2. STATISTICAL INJECTION GUARD:
   DO NOT add statistical tests if IMPLEMENTATION PLAN does not request them.
   - groupby_aggregate / pivot  ->  descriptive aggregation ONLY (no chi-square, no ANOVA, no p-values)
   - Chi-square ONLY if IMPLEMENTATION PLAN explicitly says chi-square / independence test.

3. COMPARE GUARD:
   If IMPLEMENTATION PLAN says "compare between groups" or "descriptive comparison" WITHOUT a named test:
   - Output descriptive stats per group: count, mean, median, std, min, max, p25, p75
   - Also compute absolute diff and % diff between group means
   - DO NOT add any statistical test.

4. Use ONLY columns listed in REQUIRED COLUMNS above.
   All shared rules (statistical schemas, matplotlib, pandas aliases, algorithm fidelity,
   column discipline, return structure) are in the system prompt -- follow them.

DO NOT OUTPUT:
- Placeholder decorators (def mcp_tool(), @mcp.tool())
- Mock FastMCP classes or instances
- TOOL_SPEC dicts or constants
- Explanatory text, "Here is the code:", markdown fences
- Example usage or test functions
- Import statements for the 'mcp' module
