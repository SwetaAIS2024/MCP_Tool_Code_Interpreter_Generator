Fix the following code based on the errors listed.

ORIGINAL CODE:
{code}

ERRORS TO FIX:
{errors}

TOOL SPEC:
{spec}

AVAILABLE DATASET COLUMNS (use ONLY these exact names):
{available_columns}

REPAIR RULES:
0. TRUNCATED CODE: If the error contains "TRUNCATED_OUTPUT" or the code contains
   `raise RuntimeError("TRUNCATED_OUTPUT...")`, the function was cut off mid-generation.
   Rewrite the ENTIRE function from scratch using the TOOL SPEC and IMPLEMENTATION PLAN.
   Output the complete, fully working function with all algorithm steps and final return statement.
   DO NOT keep the raise RuntimeError line.
1. Fix each error exactly as described.
   Keep function name and signature: def {function_name}(file_path: str):
2. Return {"result": {}, "metadata": {}}
   All shared rules (statistical schemas, matplotlib, pandas aliases, causal fidelity,
   sklearn compat, return structure) are in the system prompt -- follow them.
3. MISSING MODULE -- use algorithm-class-correct fallback (system prompt Algorithm Fidelity rules).
   FORBIDDEN for causal discovery:
     grangercausalitytests (NEVER a substitute for DyNOTEARS/NOTEARS -- different algorithm)
     sklearn LinearRegression, pairwise regression loops, correlation matrices.
   Correct causal fallbacks (in order):
     dynotears  -> try causallearn notears_linear; if unavailable -> IMPLEMENT MANUALLY
                   (use numpy + scipy.optimize.minimize + scipy.linalg.expm; see system prompt template)
     notears    -> causallearn notears_linear; if unavailable -> manual scipy implementation
   Non-causal missing module -> replace with pandas/numpy/scipy/statsmodels/sklearn/stdlib
     keeping the same algorithmic intent.
4. COMMON FIXES:
   - "sparse" kwarg in OneHotEncoder/LabelBinarizer  -> use sparse_output=False  (sklearn >=1.2)
   - "sparse" OR "sparse_output" kwarg in StandardScaler -> REMOVE IT entirely: StandardScaler() takes NO such kwarg
   - Invalid freq in resample()          -> 'ME' 'QE' 'YE'
   - Invalid freq in to_period()         -> 'M' 'Q' 'Y'
   - pd.to_datetime() on integer month   -> use crash_date column instead
   - KeyError: column name               -> use only AVAILABLE DATASET COLUMNS above
   - list has no .mean()                 -> np.mean(list) or pd.Series(list).mean()
   - f_oneway for 2 groups               -> ttest_ind or mannwhitneyu
   - dict(zip(tukey.pvalues, ...))       -> pd.DataFrame(tukey.summary().data[1:], columns=...)

Output ONLY the corrected Python code (imports + function). Nothing else.
